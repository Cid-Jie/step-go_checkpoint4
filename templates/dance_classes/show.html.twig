{% extends 'base.html.twig' %}

{% block title %}DanceClasses{% endblock %}

{% block body %}
<main class="container">
    <h1 class="text-center mt-5 mb-5">Cours de danses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dance_class.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dance_class.description }}</td>
            </tr>
        </tbody>
    </table>
    <p class="text-center mt-5 mb-5">
        <a href="{{ path('app_dance_classes_edit', {'id': dance_class.id}) }}" class="btn">Modifier</a>
    </p>
    {{ include('dance_classes/_delete_form.html.twig') }}

    <p class="mt-5 mb-5">
        <a href="{{ path('app_dance_classes_index') }}">Retour à la liste des cours</a>
    </p>
</main>
{% endblock %}
