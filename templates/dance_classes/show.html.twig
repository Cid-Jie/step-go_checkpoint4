{% extends 'base.html.twig' %}

{% block title %}DanceClasses{% endblock %}

{% block body %}
    <h1>DanceClasses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dance_class.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dance_class.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dance_classes_index') }}">back to list</a>

    <a href="{{ path('app_dance_classes_edit', {'id': dance_class.id}) }}">edit</a>

    {{ include('dance_classes/_delete_form.html.twig') }}
{% endblock %}
