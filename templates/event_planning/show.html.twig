{% extends 'base.html.twig' %}

{% block title %}EventPlanning{% endblock %}

{% block body %}
<main class="container">
    <h1 class="text-center mt-5 mb-5">Planning</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_planning.id }}</td>
            </tr>
            <tr>
                <th>DaysOfWeek</th>
                <td>{{ event_planning.daysOfWeek }}</td>
            </tr>
            <tr>
                <th>HoursOfDay</th>
                <td>{{ event_planning.hoursOfDay }}</td>
            </tr>
        </tbody>
    </table>
    <p class="text-center mt-5 mb-5">
       <a href="{{ path('app_event_planning_edit', {'id': event_planning.id}) }}">Modifier</a>
    </p>
    {{ include('event_planning/_delete_form.html.twig') }}

    <p class="mt-5 mb-5">
       <a href="{{ path('app_event_planning_index') }}">Retour à la liste des plannings</a>
    </p>
</main>
{% endblock %}
