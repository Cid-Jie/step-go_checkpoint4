<form method="post" action="{{ path('app_event_planning_delete', {'id': event_planning.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_planning.id) }}">
    <button class="btn">Delete</button>
</form>
