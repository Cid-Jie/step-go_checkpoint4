{% extends 'base.html.twig' %}

{% block title %}DanceTeacher{% endblock %}

{% block body %}
<main class="container">
    <h1 class="text-center mt-5 mb-5">DanceTeacher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance_teacher.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ dance_teacher.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ dance_teacher.lastname }}</td>
            </tr>
            <tr>
                <th>Story</th>
                <td>{{ dance_teacher.story }}</td>
            </tr>
        </tbody>
    </table>
    
    <p class="text-center mt-5 mb-5">
        <a href="{{ path('app_dance_teacher_index') }}">back to list</a>
    </p>
    <p class="text-center mt-5 mb-5">
        <a href="{{ path('app_dance_teacher_edit', {'id': dance_teacher.id}) }}">edit</a>
    </p>
    {{ include('dance_teacher/_delete_form.html.twig') }}
</main>
{% endblock %}
