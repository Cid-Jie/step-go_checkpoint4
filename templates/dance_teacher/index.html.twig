{% extends 'base.html.twig' %}

{% block title %}DanceTeacher index{% endblock %}

{% block body %}
<main class="container">

    <h1 class="text-center mt-5 mb-5">Professeurs</h1>

    <a href="{{ path('app_dance_teacher_new') }}">Create new</a>
    
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Story</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dance_teacher in dance_teachers %}
            <tr>
                <td>{{ dance_teacher.id }}</td>
                <td>{{ dance_teacher.firstname }}</td>
                <td>{{ dance_teacher.lastname }}</td>
                <td>{{ dance_teacher.story }}</td>
                <td>{{ dance_teacher.danceClasses.name }}</td>
                <td>
                    <a href="{{ path('app_dance_teacher_show', {'id': dance_teacher.id}) }}">show</a>
                    <a href="{{ path('app_dance_teacher_edit', {'id': dance_teacher.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</main>
{% endblock %}
