<form method="post" action="{{ path('app_repeated_event_delete', {'id': repeated_event.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repeated_event.id) }}">
    <p class="text-center mt-5 mb-5">
        <button class="btn">Effacer</button>
    </p>
</form>
